# 宝贝英语学习站 · 更新日志

## 2026-08-25 v2 更新：美化 + 自动打卡

### 打卡功能重构

**旧版**：顶部独立打卡按钮，孩子点一下算打卡，和视频播放完全脱节。

**新版（今日任务模式）**：
- 顶部变成「今日任务卡片」，点卡片直接打开当天要看的视频
- 每天自动分配一个任务（按周轮换分类：动画→儿歌→拼读→词汇，不重复）
- **看完或关闭播放器 → 自动打卡 + 庆祝弹层 + 礼花特效**
- 已打卡状态：卡片变绿，显示「✅ 今日已完成」
- 切换年级时自动切换对应年级的任务和记录

**B站视频打卡逻辑**：播放器关闭时打卡（无法精确检测是否看完），防止孩子中途退出也可以接受，毕竟能打开就算开始了。

**本地 mp4 视频**：严格监听 `onended` 事件，必须完整播放完才打卡。

### 排版美化

- 背景：从纯渐变改成「顶部彩色圆弧 + 白色底部」，更清爽
- 打卡卡片：大圆角、阴影变粉紫色（任务）/ 绿色（已完成）
- 打卡按钮彻底取消，改成任务导向
- 视频卡片：已完成的视频右上角出现绿色 ✓ 标记
- 庆祝弹层：弹出动画 + emoji + 连击天数文案 + 礼花粒子
- 周进度条：每天一个小圆点，完成✓ / 今天脉冲动画 / 未到灰色

### 自动打卡流程

```
点「今日任务卡片」
    ↓
打开播放器，看视频
    ↓
看完 / 关闭播放器 / 切换页面
    ↓
自动打卡 → 刷新UI → 弹出庆祝弹层 → 礼花 🎉
```

### 新增用户数据存储

- `ci_小学` / `ci_初中`：各自年级的打卡日期数组（JSON，存浏览器 localStorage）
